# 第三轮真机交接：π0.5 多候选 rollout + 传感器标定（2026-09-13）

**给谁看**：采集台 / 真机那边。**自包含**，照着做即可。
**顺序建议**：§1 标定（20 分钟，不动机器人的那部分可以先做）→ §2 回放重复性（10 分钟）→ §3 π0.5 rollout × 30。
§1 不阻塞 §3：rollout 录下的原始 F6 事后可以用新标定重新算，但**标定要和 rollout 在同一天、同一次上电**做，
否则零点和传感器状态不一样。

---

## 0. 这一轮要交回来的东西

| # | 交付 | 用途 |
|---|---|---|
| 1 | 标定：5 条按压 episode + 2 条自由空间 episode + `calib_sharpa_left.json` + `calib_new_rig.py` 的打印输出 | 解锁法向力 / 切向力 / 接触标签 |
| 2 | 回放重复性：同一条轨迹回放 5 遍的录制 | 反事实实验（B 块）能不能做的 go/no-go |
| 3 | 30 次 π0.5 rollout，每次带 N 个候选、全程触觉、成功/失败标签 | predictor 的训练主力 + scorer 的检验集 |

---

## 1. 传感器标定（~20 分钟）

### 1.1 为什么要做

现在自采数据用的**法向表、接触阈值（0.05 N）、切向比的法向下限（1.0 N）全是 T-Rex 那台设备的值**。
F6 是传感器自己坐标系下的 6 维力，要拆成「法向力（捏多紧）」和「切向力（快滑了没）」，必须知道每根手指的法向。
传感器坐标系偏离 z 轴 27–48°，直接取 z 分量会让**纯法向压紧也读出 0.7 的切向比**。
不标定的后果是所有逐指物理量静默偏掉，**不会报错**。

### 1.2 准备

- 一块**刚性平板**：铝板 / 5 mm 以上亚克力 / 硬木板，至少 10×10 cm，表面平整不打滑（贴一层薄胶带也行）。**不要用软的东西**（泡沫、橡胶垫）。
- 触觉 tap 要在跑：Sharpa Pilot 开着，`:50011` 有进程占着（`collect_up.sh preflight` 里这一项必须绿）。
- 手上电，用平常的采集链路录 episode（`collect_up.sh up` + Quest A 开始/结束），**task 名写清楚**，例如
  `calib_press_thumb`、`calib_free_start`。每条 episode 单独一个文件。
- 臂可以不动：手保持在一个张开、各指指腹朝外可够到的姿态即可（home 姿态手张开通常就行）。

### 1.3 录什么（按这个顺序）

| # | episode | 做法 | 时长 |
|---|---|---|---|
| **A-2** | `calib_free_start` | 手张开，**全程不碰任何东西**。可以让手在空中慢慢动一动 | 60 s |
| **A-1 ×5** | `calib_press_thumb` / `_index` / `_middle` / `_ring` / `_pinky` | **每根手指单独一条**。拿平板**垂直**压向这根手指的指腹：力从 0 **慢慢**加到约 20 N（用力捏一个硬物的程度），停 1 s，再**慢慢**卸掉。**压 3 秒、松 2 秒**，重复 10 次 | 每条 ~60 s |
| **A-3** | `calib_free_end` | 同 A-2，放在**当天所有录制的最后**（rollout 做完之后） | 60 s |

### 1.4 三条会静默出错的细节（最重要）

1. **只能正压**：平板垂直于指腹压下去，**不要蹭、不要滑、不要斜着压**。法向就是按「平均受力方向」算的，
   你怎么压就会算出什么方向；带了横向的蹭，算出来的数值**看起来完全正常**，只有下面的门 2 能发现。
2. **每次之间一定要完全松开**（压 3 s / 松 2 s）。零点是用「力最小的 10% 的帧」估的，连着压不松手，零点会被估高。
3. **不能用手指戳代替平板**：戳产生不了垂直压入的方向，也到不了 20 N。
4. 其余四根手指保持不碰任何东西（它们的读数就是那条 episode 的噪声底）。

### 1.5 录完立刻跑验收（台子别拆）

`calib_new_rig.py` 只依赖 `numpy + h5py`，不需要 GPU。把 `tactile_steering/calib_new_rig.py` 和 `tactile_steering/calib.py`
两个文件拷到采集机同一目录下即可：

```bash
python calib_new_rig.py --protocol      # 先看一遍操作步骤

python calib_new_rig.py \
    --press thumb=<calib_press_thumb>.hdf5 index=<..._index>.hdf5 middle=<..._middle>.hdf5 \
            ring=<..._ring>.hdf5 pinky=<..._pinky>.hdf5 \
    --free-start <calib_free_start>.hdf5 --free-end <calib_free_end>.hdf5 \
    --side left --out calib_sharpa_left.json
```

| 门 | 通过标准 | 不过怎么办 |
|---|---|---|
| 0 通道死活 | 5 路都在推数据 | 查线 / Pilot |
| 1 通道↔手指 | 压哪根手指就亮哪一路，且构成置换 | 映射 8/20 已经测定，应当直接过；不过就停下来找我们 |
| 2 **每指方向集中度 R > 0.90** | 压得够「正」 | **重录那根手指的 A-1**，这次更注意垂直、不蹭 |
| 3 `log|F|` 直方图双峰、谷底清晰 | 能定接触阈值 | 检查是否压到了 ~20 N、中间是否完全松开 |
| 4 A-3 − A-2 零点差 ≤ 0.5 N | 当天漂移可接受 | 记下来发我们（已知有迟滞，不一定要重做） |

**任何一道门没过，先修好再往下录。** 把终端打印结果、`calib_sharpa_left.json`、7 条 hdf5 一起传回来。

### 1.6 可选：单位核对（多 5 分钟，强烈建议）

`units_note` 里 F6 的单位是**「推断的 N / Nm，Sharpa 没有文档」**。如果手边有厨房秤：秤放在桌上、
上面放平板，遥操作让**一根手指的指腹垂直压在平板上**，力慢慢加到秤读数约 1 kg、2 kg 各停 3 s，
同时录一条 `calib_units_index`，并**把每次停住时秤的读数（g）记在 notes 里**。
我们拿 |F| 和秤读数（g × 9.81 / 1000 = N）对一下，就能确认单位是不是真的 N。

---

## 2. 回放重复性（~10 分钟）—— 反事实实验的 go/no-go

以后要做「同一个状态、执行不同候选」的反事实实验（B 块），前提是**复位 + 回放足够准**：
候选之间的差异只有毫米级，而 IK 跟踪误差 p90 是 10.5 mm（相对指令的误差，可能大部分是系统性的、可以抵消），**重复性从来没测过**。

- 选一条成功的 teleop episode，用 `collect/replay_episode.py` **在同一个物体位置上回放 5 遍**（每遍之间物体和机器人都复位）。
- 每遍都录（和普通 episode 一样录）。
- 我们算：同一时刻 5 遍之间末端位置的散布、F6 的散布。

| 5 遍之间末端散布 | 结论 |
|---|---|
| < 1.5 mm | 反事实实验走主方案 |
| 1.5–5 mm | 边缘，扰动幅度要重新定 |
| > 5 mm | 候选之间的差异会被复位误差淹掉，要改设计 |

---

## 3. π0.5 多候选 rollout × 30

### 3.1 服务端（GPU 那台）

openpi 的 `sharpa-rollout` 分支新增了「一次采 N 个候选、可调噪声温度」的推理入口（`src/openpi/policies/candidate_policy.py`，
协议见 `examples/sharpa/rollout/README.md`）。**默认 N=1、T=1.0，行为与之前逐位相同。**

```bash
cd $OPENPI && git pull          # 拿到 sharpa-rollout 的新提交
scripts/sharpa_serve.sh ./checkpoints/sharpa-pi05/egg_70ep_b64/15120 sharpa_egg \
    --num-candidates 4 --noise-temperature 1.0
```

- **先测延迟**：`ping_policy.py` 要改成请求里带 `num_candidates`（或者服务端直接用上面的默认值）。
  预算是 267 ms（`--infer-lead 8 / --fps 30`）。**用这个 checkpoint 在 A100-80GB 上实测**
  （`tactile_steering/dp2/out/dp2b_candidates.md`，2026-09-13）：

  | N | 1 | 2 | 4 | 8 | 16 |
  |---|---|---|---|---|---|
  | 中位 / 最大（ms） | 92 / 93 | 128 / 161 | **193 / 235** | **310 / 324** | 558 / 591 |

  ⇒ **默认用 N=4**，在预算内。要用 N=8 就得把 `--infer-lead` 加到 **≥ 10**（333 ms），代价是每个 chunk
  实际执行的步数更少。真机那张卡如果比 A100 慢，这些数都会更大，**以 `ping_policy.py` 的实测为准**。
  **N 定下来之后 30 次都不要改。**
- **候选之间的差异在 T=1 时很小**（同一份实测，N=8，24 帧）：

  | T | 候选之间腕部终点相差 | 手指关节相差 | 离演示动作的腕部误差 |
  |---|---|---|---|
  | 0.5 | 0.75 mm | 0.0015 rad | 1.7 mm |
  | **1.0** | **1.8 mm** | **0.0028 rad**（约 0.16°） | 2.2 mm |
  | 1.5 | 3.3 mm | 0.0061 rad | 3.1 mm |
  | 2.0 | 5.4 mm | 0.0165 rad | 5.0 mm |

  这批 30 次仍然固定 T=1.0（先拿到模型自己采样器下的基线）；温度扫描见 §4 第 3 条。
- **每换一个 N，第一次调用要 JIT 编译几十秒**：机器人使能之前，用实际的 N 预热一次。
- **温度这 30 次固定 T=1.0**（模型自己的采样器）。温度扫描放到之后单独做，不要和这批混在一起。

### 3.2 每个 chunk 边界怎么选

- 从 N 个候选里**均匀随机选一个执行**（random-of-N）。随机数用**每条 episode 一个固定 seed**，seed 记下来。
- **不要**总是执行候选 0，也不要挑「看起来最好」的：我们要的就是「在相似状态下，执行哪个动作是随机的」，
  这样动作带来的触觉差异才能和状态本身区分开。

### 3.3 录制：客户端 / 录制代码要加、要改的（在 `openarm_track/rollout/`）

**(a) 触觉必须录上。** RUNBOOK 第 6 步说「`nothing owns :50011 -- Sharpa Pilot is not running` 可以忽略，base policy 不吃触觉」——
**这一轮不能忽略**：policy 不吃触觉，但我们要录触觉。Pilot 和 tap 必须在跑，F6 **和形变图**都要录（T-Rex 那 11 次没有形变图）。

**(b) 录制频率统一 30 Hz。** T-Rex 那 11 次 rollout 是 15 Hz（前 2 次是 30 Hz）；`flight.npz` 是每个控制步写一行，
所以录制频率就是控制频率。要么控制循环跑 30 Hz，要么**传感器单独一个线程按 30 Hz 录**，不受控制频率影响。
最好直接复用采集器的写入器（rt-v1 schema），这样 rollout 和演示是同一种格式、同一套转换脚本。

**(c) 每个 chunk 边界新增这些字段**（K = 这条 episode 的 chunk 边界数）：

| 字段 | 形状 | 含义 |
|---|---|---|
| `cand/obs_step` | (K,) | 这次推理用的是第几个控制步的观测 |
| `cand/actions` | (K, N, 30, 28) | 服务端返回的 `actions_candidates`（全部 N 个） |
| `cand/noise` | (K, N, 30, 32) | 服务端返回的 `candidate_noise`（可以复现每个候选） |
| `cand/chosen` | (K,) | 实际执行的是第几个 |
| `cand/exec_steps` | (K, 2) | 这个 chunk 实际从第几步执行到第几步（`--infer-lead` 会让下一个 chunk 提前接管，**实际执行不到 30 步**） |
| `cand/infer_ms` | (K,) | 服务端的推理耗时 |
| attrs | — | `num_candidates`、`noise_temperature`、`selection="uniform_random"`、`selection_seed`、checkpoint 路径、openpi 和 rig 的 git commit |

数据量很小：N=8 时每个边界约 30 KB。

**(d) 首尾各留 1–2 秒自由空间**：第一次推理前让手张开静止 1–2 s，结束松开后再录 1–2 s，
全程不碰任何东西。F6 的零点就从这里估（有迟滞，**每条 episode 单独估**）。

**(e) 结束时当场标注**：按键或终端菜单，选
`success` / `grasp_miss` / `grasp_miss_knocked_over` / `slip_during_lift` / `slip_after_lift` / `other`（other 要写一句话），
自动写进这条 episode 的元数据。**不要事后凭记忆补。**

**(f) 这些事件也要记下来**：安全包络触发（`SAFETY STOP` 的原因和步号）、`p` 暂停和恢复的步号、`stale observation` 停止。
这些 episode 照常保存、照常标注，不要删。

**(g) 鸡蛋起始位置**：桌上用胶带标一个网格（例如 3×3、间距 3 cm，以训练时的位置为中心），
每次记下放在哪一格。场景相机的首帧也会存下来，但网格编号更好用。
**不要超出这个范围**：训练数据里鸡蛋的位置散布只有几厘米，放太远 π0.5 基本全失败，没有信息量。

### 3.4 这 30 次还要注意的

- **30 次全部用同一个 checkpoint（15120）、同一个 N、T=1.0、同一套场景和光照。** 一次只变一个量。
- 各网格位置**轮流**放，不要先把一个位置跑完再换下一个（光照、手的温度、操作手法都会漂，连着跑会把漂移算到位置头上）。
- 失败的也要跑完整条，**不要中途放弃重来**：失败的过程和时刻正是 scorer 要学的。
- 预期成功率约 20%（上次 2/10），30 次约 6 次成功。**够用**：我们要的是动作的变化和真实结果，不需要高成功率。
- 录完先传一两条回来，我们确认格式对了，再继续跑剩下的。

---

## 4. 还有的（按优先级）

1. **把 T-Rex 的 rollout 也改成同样的录制格式**（30 Hz、带形变图、当场标注）。以后 T-Rex 和 π0.5 的 rollout 可以一起用。
2. **B 块反事实实验**：等 §2 的重复性数据出来再定。它是唯一能直接检验「模型给候选的排序对不对」的数据。
3. **温度扫描**：离线实测（`tactile_steering/dp2/dp2b_candidates.py`）会给出候选散布随 T 的变化；
   之后在真机上挑 2–3 个 T，各跑 10 次，看单个候选的成功率掉多少。
4. **补演示时加大鸡蛋位置的随机化**，尤其是左右和前后两个方向（现在主要只在一个方向上有散布）。
